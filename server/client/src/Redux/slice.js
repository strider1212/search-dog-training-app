import { createSlice } from '@reduxjs/toolkit'

export const usernameToStateSlice = createSlice({
  name: 'usernameToState',
  initialState: '',
  reducers: {
    usernameToStateReducer: () => {

    }
  }
})